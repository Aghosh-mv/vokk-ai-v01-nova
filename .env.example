# Fill any (or all) of these. Nova will auto-pick the first one it finds,
# or you can force one with DEFAULT_PROVIDER below.

GEMINI_API_KEY=
GROQ_API_KEY=
OPENROUTER_API_KEY=

# Optional — enables /search and /web on
SERPAPI_KEY=

# Optional — gemini | groq | openrouter
DEFAULT_PROVIDER=gemini
